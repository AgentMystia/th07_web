# Review targets — 2026-07-10 bomb/shot/slowmo/popups batch

Commit `eb976e4`, deployed to gh-pages (`cfb6762`). Implemented from decoded
Th07.exe specs (all in `reference/re-specs/spec-*.md`, RE'd this session).
`npm run check` clean, `npm test` 58/58, dev-shot checkpoints 120/800/2500/
5600 in band, border-probe matches the handoff.

**Read this only if reviewing the items below.** Each row = the specific
risk + the one check that confirms or refutes it. Nothing here is known-
broken; these are the spots where I (weaker model, single-modal) couldn't
fully runtime-verify and where a wrong assumption would be subtle.

## P1 — could affect gameplay correctness

1. **Effect-handler bullet-shape filters** (`eclvm.ts` ids 1/2/4/6/12/21).
   The exe filters on bullet "shape" (+0x1d4); the port uses `b.sprite`.
   If the port's shape↔sprite mapping differs from the exe's, these delete
   the wrong bullets (or none) on stage 2/3/5. **Check:** play stage 2/3/5
   past the op121(2/4/6/12/21) calls and confirm bullets clear where the
   original does.

2. **Slow-motion under a live ECL effect-10** (`stage-scene.ts`,
   `eclvm.ts`, `player.ts`, `anm.ts`, `std.ts`). Many timers now accumulate
   `slowRate` fractionally. The ECL-clock + bullet-rescale path has a unit
   test; the per-subsystem rate plumbing (enemy move, lasers, items, player
   cadence) is structurally right but **not runtime-verified** — my probes
   never trigger an actual effect-10 (stage 5/6 only). **Check:** a stage
   5/6 slowmo phase plays at the right speed with no jitter/desync.

3. **MarisaB laser release on focus-toggle** (`stage-scene.ts tickLaserSlots`
   + `anchorBeamBullet`). The waitInt-gated interrupt + 3-slot handoff is
   the most intricate path. **Check:** fire MarisaB unfocused, toggle focus
   mid-beam, confirm the old beams fade and the focused beam spawns cleanly
   (no orphan slots, no stuck beams).

## P2 — visual / cosmetic, low gameplay risk

4. **Per-form bomb sub-particle visuals** (`player-bombs.ts`). Damage,
   radius, spawn cadence are exe-confirmed; the decorative sub-particle
   placement (MarisaB sweep delta `frame²·π/18000`, MasterSpark spark
   growth `K`) is hand-tuned where the spec flagged it as ANM-baked.
   **Check:** each bomb looks roughly right vs. the original; damage/clear
   behavior is already verified by the slot engine.

5. **Popups** (`stage-scene.ts drawPopups`). Distance-from-player alpha
   pulse + glyph placement per spec, but single-modal — legibility not
   visually confirmed. The case-2 bigPower-at-max "uninitialized garbage"
   path draws nothing by deliberate choice (spec §6). **Check:** point/
   cherry/sweep popups render and fade correctly in play.

6. **Power/Point sidebar rows** (`stage-scene.ts drawSidebar`). Power bar
   is the exe gradient quad + "MAX"; Point row shows `collected/threshold`
   with the extend ladder. The slash is drawn as a text glyph (the ascii
   slash sprite wasn't positively identified). **Check:** row reads right;
   a point-extend awards a life/bomb + se_extend.

## Not reviewed, intentionally

- SakuyaB per-frame Cherry drain (spec-bombs-sakuya §10) — flagged for the
  cherry/border owner; `cherry.ts onBomb()` already had its fabricated
  lump-sum removed in a prior pass, the progressive drain is not yet wired.
- Bomb shared tint (FUN_00407520) / activation VM (FUN_00407620) —
  represented by the character bomb ANM scripts, not byte-exact VM spawns.
